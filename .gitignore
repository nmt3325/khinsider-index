# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

scripts/venv
__pycache__/
*.pyc

# metadata pipeline artefacts - these live in GitHub releases, not in git
album-list.ndjson
album-list.pages
album-list-failures.log
facet-*.ndjson
facet-*.entities
facet-*-failures.log
album-meta.ndjson
album-meta-failures.log
library.json
library.json.gz
release-notes.md
residual.txt
